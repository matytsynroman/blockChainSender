spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blockchainSender
    username: blockchain_sender
    password: blockchain_sender
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect